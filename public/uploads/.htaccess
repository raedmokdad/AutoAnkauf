# Verzeichnis-Listing blockieren
Options -Indexes

# Bilder und HTML erlauben
<FilesMatch "\.(jpg|jpeg|png|gif|webp|json|html|htm)$">
    Require all granted
</FilesMatch>

# PHP-Ausführung verhindern
<FilesMatch "\.php$">
    Require all denied
</FilesMatch>
